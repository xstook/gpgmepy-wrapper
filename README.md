# gpgmepy-wrapper
This is a wrapper class and functions to work with GPG and GPGME

The tests.py is a good reference for how to interact with the functions

# Installation
Run the `install-gpg-bindings.sh` script to install and setup the necessary dependencies
